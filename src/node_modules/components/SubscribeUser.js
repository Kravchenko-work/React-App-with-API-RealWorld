import React from "react";
import useFetch from "hooks/useFetch";

const SubscribeUser = ({userName}) => {
    const api = `/profiles/${userName}/follow`;
    const [{response, isLoading, error}, doFetch] = useFetch(api);

    const handleClick = () => {

    }

    return (
        <span>
            <button className='btn btn-sm action-btn btn-outline-secondary' onClick={handleClick}>
                <i className='ion-plus-round'></i>
                {`\tFollow ${userName}`}
            </button>
        </span>
    )
}

export default SubscribeUser;